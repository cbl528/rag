spring:
  application:
    name: business
  profiles:
    active: dev
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

# ===== 对象存储（MinIO） =====
# 生产环境需要填写 endpoint/accessKey/secretKey/bucket，
# 敏感配置请在 application-dev.yaml 中覆盖。
minio:
  endpoint: http://124.221.110.104:9000
  access-key: minioadmin
  secret-key: minioadmin

# ===== 本地文件上传路径（头像等） =====
upload:
  local-path: ./uploads


